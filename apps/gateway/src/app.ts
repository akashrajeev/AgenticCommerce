import { createHash } from "node:crypto";
import type { PurchaseIntent, Transaction } from "@mandate/types";
import { createHealthStatus } from "@mandate/shared";
import cors from "cors"; import express from "express"; import helmet from "helmet";
import { config } from "./config.js";
import { getRealizedRevenueAttribution, initializePersistence, isDatabaseConfigured, listMerchantOrders, saveMerchantOrder } from "./persistence.js";
import { claimWebhookEvent, releaseWebhookEvent } from "./persistence-prisma.js";
import { addWebhookAudit, attachRazorpayOrder, confirmOrder, findTransactionByRazorpayOrderId, flushPersistence, getAllTransactions, getTimeline, getTransaction, hydrateTransactionStore, markCheckoutStarted, proposeTransaction, recordPayment, recordPaymentVerified, retryFailedTransaction } from "./transaction-core.js";
import { capturePayment, createOrder, fetchOrderPayments, fetchPayment, getPublicConfig, verifyCheckoutSignature, verifyWebhookSignature } from "./razorpay.js";
const processedWebhookKeys = new Set<string>(); const processingWebhookKeys = new Set<string>();
function parsePurchaseIntent(body: unknown): Omit<PurchaseIntent, "id"> { if (!body || typeof body !== "object") throw new Error("INVALID_REQUEST"); const input = body as Record<string, unknown>; const merchantId = typeof input.merchantId === "string" ? input.merchantId : ""; const productId = typeof input.productId === "string" ? input.productId : ""; const quoteId = typeof input.quoteId === "string" ? input.quoteId : ""; const reason = typeof input.reason === "string" ? input.reason.trim() : ""; const quantity = typeof input.quantity === "number" ? input.quantity : NaN; const maxSpendPaise = typeof input.maxSpendPaise === "number" ? input.maxSpendPaise : NaN; const rawLineItems = Array.isArray(input.lineItems) ? input.lineItems : undefined; const lineItems = rawLineItems?.map((item) => { if (!item || typeof item !== "object") throw new Error("INVALID_LINE_ITEMS"); const line = item as Record<string, unknown>; const id = typeof line.productId === "string" ? line.productId : ""; const qty = typeof line.quantity === "number" ? line.quantity : NaN; if (!id || !Number.isInteger(qty) || qty < 1 || qty > config.maxQuantity) throw new Error("INVALID_LINE_ITEMS"); return { productId: id, quantity: qty }; }); if (!merchantId || !productId || !quoteId || !reason) throw new Error("INVALID_REQUEST"); if (!Number.isInteger(quantity) || quantity < 1 || quantity > config.maxQuantity) throw new Error("INVALID_QUANTITY"); if (!Number.isInteger(maxSpendPaise) || maxSpendPaise < 0) throw new Error("INVALID_SPEND_LIMIT"); if (lineItems && (lineItems.length < 1 || lineItems.length > 10)) throw new Error("INVALID_LINE_ITEMS"); const effective = lineItems ?? [{ productId, quantity }]; const totalQuantity = effective.reduce((sum, item) => sum + item.quantity, 0); if (totalQuantity > config.maxQuantity) throw new Error("INVALID_QUANTITY"); const firstLine = lineItems?.[0]; if (firstLine && (firstLine.productId !== productId || firstLine.quantity !== quantity)) throw new Error("INVALID_LINE_ITEMS"); return { merchantId, productId, quoteId, reason, quantity, maxSpendPaise, ...(lineItems ? { lineItems } : {}) }; }
function getTransactionOrThrow(id: string): Transaction { const transaction = getTransaction(id); if (!transaction) throw new Error("TRANSACTION_NOT_FOUND"); return transaction; }
async function confirmMerchantOrder(transaction: Transaction) { if (!transaction.razorpayOrderId || !transaction.razorpayPaymentId) throw new Error("PAYMENT_REFERENCE_INCOMPLETE"); const base = config.merchantInternalUrl.replace(/\/$/, ""); const response = await fetch(`${base}/api/agent/orders/confirm`, { method: "POST", headers: { "content-type": "application/json", "x-mandate-gateway-secret": config.internalGatewaySecret }, body: JSON.stringify({ transactionId: transaction.id, productId: transaction.intent.productId, quantity: transaction.intent.quantity, lineItems: transaction.intent.lineItems, amountPaise: transaction.quote.totalPaise, razorpayOrderId: transaction.razorpayOrderId, razorpayPaymentId: transaction.razorpayPaymentId }) }); if (!response.ok) throw new Error("MERCHANT_ORDER_CONFIRMATION_FAILED"); const body = await response.json().catch(() => null) as { order?: { id?: unknown; productId?: unknown; quantity?: unknown; lineItems?: unknown; amountPaise?: unknown; currency?: unknown; baseAmountPaise?: unknown; incrementalRevenuePaise?: unknown } } | null; const order = body?.order; if (!order || typeof order.id !== "string") throw new Error("MERCHANT_ORDER_CONFIRMATION_INVALID"); const lineItems = Array.isArray(order.lineItems) ? order.lineItems : transaction.quote.lineItems; const firstLine = Array.isArray(lineItems) && lineItems.length > 0 ? lineItems[0] as Record<string, unknown> : undefined; const firstLineTotal = typeof firstLine?.lineTotalPaise === "number" ? firstLine.lineTotalPaise : transaction.quote.totalPaise; const merchandiseTotal = Array.isArray(lineItems) ? lineItems.reduce((sum, line) => sum + (typeof (line as Record<string, unknown>).lineTotalPaise === "number" ? Number((line as Record<string, unknown>).lineTotalPaise) : 0), 0) : transaction.quote.totalPaise; const baseAmountPaise = typeof order.baseAmountPaise === "number" ? order.baseAmountPaise : firstLineTotal; const incrementalRevenuePaise = typeof order.incrementalRevenuePaise === "number" ? order.incrementalRevenuePaise : Math.max(merchandiseTotal - baseAmountPaise, 0); await saveMerchantOrder({ merchantOrderId: order.id, transactionId: transaction.id, merchantId: transaction.intent.merchantId, productId: typeof order.productId === "string" ? order.productId : transaction.intent.productId, quantity: typeof order.quantity === "number" ? order.quantity : transaction.intent.quantity, lineItems: lineItems as Transaction["quote"]["lineItems"], amountPaise: typeof order.amountPaise === "number" ? order.amountPaise : transaction.quote.totalPaise, baseAmountPaise, incrementalRevenuePaise, currency: typeof order.currency === "string" ? order.currency : transaction.quote.currency, razorpayOrderId: transaction.razorpayOrderId, razorpayPaymentId: transaction.razorpayPaymentId, createdAt: new Date().toISOString() }); return body; }
async function reconcileCapturedPayment(transactionId: string, paymentId: string, source: "checkout" | "webhook") { const transaction = getTransactionOrThrow(transactionId); if (transaction.state === "order_confirmed") { if (source === "webhook") addWebhookAudit(transactionId, "WEBHOOK_RECONCILIATED", "Razorpay confirmed a captured payment for an already-confirmed transaction; no state change was required.", { razorpayPaymentId: paymentId, source, alreadyConfirmed: true }); return transaction; } recordPayment(transactionId, paymentId, "captured"); if (getTransaction(transactionId)?.state === "payment_captured") recordPaymentVerified(transactionId); const verified = getTransactionOrThrow(transactionId); if (verified.state !== "payment_verified" && verified.state !== "order_confirmed") return verified; addWebhookAudit(transactionId, source === "webhook" ? "WEBHOOK_RECONCILIATED" : "CHECKOUT_RECONCILIATED", source === "webhook" ? "Razorpay webhook confirmed the captured payment and triggered reconciliation." : "Browser checkout callback was verified and triggered payment reconciliation.", { razorpayPaymentId: paymentId, source }); if (verified.state === "payment_verified") await confirmMerchantOrder(verified); return confirmOrder(transactionId); }
async function captureAndReconcile(transactionId: string, paymentId: string, amountPaise: number) { const transaction = getTransactionOrThrow(transactionId); if (transaction.state === "order_confirmed") return transaction; const payment = await fetchPayment(paymentId); if (payment.status === "failed") { recordPayment(transactionId, payment.id, "failed"); return getTransactionOrThrow(transactionId); } if (payment.status === "authorized") { const captured = await capturePayment(payment.id, amountPaise); if (captured.status === "captured") return reconcileCapturedPayment(transactionId, captured.id, "checkout"); recordPayment(transactionId, captured.id, "authorized"); return getTransactionOrThrow(transactionId); } if (payment.status === "captured") return reconcileCapturedPayment(transactionId, payment.id, "checkout"); return getTransactionOrThrow(transactionId); }
export function createApp() { const app = express(); app.use(helmet()); app.use(cors({ origin: [config.buyerAppOrigin, config.merchantAppOrigin] })); app.use((_request, response, next) => { const originalJson = response.json.bind(response); response.json = ((body: unknown) => { void flushPersistence().then(() => originalJson(body)).catch((error) => { console.error("Persistence flush failed before response", error); response.status(503); originalJson({ error: "PERSISTENCE_UNAVAILABLE" }); }); return response; }) as typeof response.json; next(); }); let persistenceReady: Promise<void> | undefined; app.use(async (_request, _response, next) => { try { if (!persistenceReady) persistenceReady = initializePersistence().then((seed) => hydrateTransactionStore(seed)); await persistenceReady; next(); } catch (error) { next(error); } });
app.post("/v1/webhooks/razorpay", express.raw({ type: "application/json", limit: "256kb" }), async (request, response) => { let dedupeKey: string | undefined; let claimedProcessing = false; let persistentClaimed = false; try { const rawBody = Buffer.isBuffer(request.body) ? request.body.toString("utf8") : ""; const signature = request.header("x-razorpay-signature") ?? ""; if (!rawBody || !verifyWebhookSignature(rawBody, signature)) return response.status(400).json({ error: "INVALID_WEBHOOK_SIGNATURE" }); dedupeKey = request.header("x-razorpay-event-id") ?? createHash("sha256").update(rawBody).digest("hex"); if (processedWebhookKeys.has(dedupeKey)) return response.status(200).json({ received: true, duplicate: true }); if (processingWebhookKeys.has(dedupeKey)) return response.status(200).json({ received: true, duplicate: true, processing: true }); processingWebhookKeys.add(dedupeKey); claimedProcessing = true; const payload = JSON.parse(rawBody) as { event?: string; payload?: { payment?: { entity?: { id?: string; order_id?: string } }; order?: { entity?: { id?: string } } } }; const event = payload.event ?? "unknown"; const payment = payload.payload?.payment?.entity; const orderId = payment?.order_id ?? payload.payload?.order?.entity?.id; persistentClaimed = await claimWebhookEvent({ dedupeKey, eventName: event, razorpayOrderId: orderId ?? null, razorpayPaymentId: payment?.id ?? null }); if (!persistentClaimed) { processedWebhookKeys.add(dedupeKey); return response.status(200).json({ received: true, duplicate: true }); } const transaction = orderId ? findTransactionByRazorpayOrderId(orderId) : undefined; if (transaction) addWebhookAudit(transaction.id, "WEBHOOK_RECEIVED", `Razorpay delivered ${event}.`, { event, razorpayOrderId: orderId ?? null, razorpayPaymentId: payment?.id ?? null, webhookEventId: dedupeKey }); if (transaction && payment?.id) { if (event === "payment.failed") recordPayment(transaction.id, payment.id, "failed"); else if (event === "payment.authorized") recordPayment(transaction.id, payment.id, "authorized"); else if (event === "payment.captured") await reconcileCapturedPayment(transaction.id, payment.id, "webhook"); } else if (transaction && event === "order.paid") { const payments = await fetchOrderPayments(transaction.razorpayOrderId!); const captured = payments.items.find((item) => item.status === "captured"); if (captured) await reconcileCapturedPayment(transaction.id, captured.id, "webhook"); } processedWebhookKeys.add(dedupeKey); return response.status(200).json({ received: true, event, matchedTransaction: Boolean(transaction) }); } catch (error) { console.error("Razorpay webhook processing failed", error); if (persistentClaimed && dedupeKey) await releaseWebhookEvent(dedupeKey).catch(() => undefined); return response.status(500).json({ error: "WEBHOOK_PROCESSING_FAILED" }); } finally { if (claimedProcessing && dedupeKey && !processedWebhookKeys.has(dedupeKey)) processingWebhookKeys.delete(dedupeKey); } });
app.post("/v1/webhooks/lab/replay", express.json({ limit: "8kb" }), async (request, response) => { const providedSecret = request.header("x-mandate-gateway-secret") ?? ""; if (!providedSecret || providedSecret !== config.internalGatewaySecret) return response.status(401).json({ error: "UNAUTHORIZED_WEBHOOK_LAB" }); const transactionId = typeof request.body?.transactionId === "string" ? request.body.transactionId.trim() : ""; if (!transactionId) return response.status(400).json({ error: "TRANSACTION_ID_REQUIRED" }); try { const transaction = getTransactionOrThrow(transactionId); const dedupeKey = `lab_${transactionId}_${Date.now()}_${crypto.randomUUID().replaceAll("-", "").slice(0, 8)}`; const stateBefore = transaction.state; const payload = { eventName: "payment.captured", razorpayOrderId: transaction.razorpayOrderId ?? null, razorpayPaymentId: transaction.razorpayPaymentId ?? null }; const firstDelivery = await claimWebhookEvent({ dedupeKey, ...payload }); const secondDelivery = await claimWebhookEvent({ dedupeKey, ...payload }); addWebhookAudit(transactionId, "WEBHOOK_REPLAY_LAB", "Synthetic webhook replay test delivered the same event identity twice; transaction state was intentionally left unchanged.", { dedupeKey, firstDelivery: firstDelivery ? "ACCEPTED" : "DUPLICATE", secondDelivery: secondDelivery ? "ACCEPTED" : "DUPLICATE", databaseBacked: isDatabaseConfigured() }); if (!secondDelivery) addWebhookAudit(transactionId, "WEBHOOK_REPLAY_BLOCKED", "Duplicate webhook delivery was rejected by the persistent idempotency boundary.", { dedupeKey }); return response.json({ transactionId, dedupeKey, firstDelivery: firstDelivery ? "ACCEPTED" : "DUPLICATE", secondDelivery: secondDelivery ? "ACCEPTED" : "DUPLICATE", databaseBacked: isDatabaseConfigured(), stateBefore, stateAfter: getTransactionOrThrow(transactionId).state }); } catch (error) { const message = error instanceof Error ? error.message : "WEBHOOK_REPLAY_FAILED"; return response.status(message === "TRANSACTION_NOT_FOUND" ? 404 : 422).json({ error: message }); } });
app.use(express.json({ limit: "32kb" })); app.get("/health", (_request, response) => response.json(createHealthStatus("gateway"))); app.get("/", (_request, response) => response.json({ name: "MANDATE Gateway", role: "deterministic policy, transaction, Razorpay, webhook, and audit boundary", status: "razorpay-test-ready", testMode: true })); app.get("/v1/transactions", (_request, response) => response.json({ transactions: getAllTransactions() })); app.get("/v1/merchant/orders", (_request, response) => response.json({ orders: listMerchantOrders(), revenue: getRealizedRevenueAttribution() })); app.post("/v1/purchase-intents", async (request, response) => { try { response.status(201).json({ transaction: await proposeTransaction(parsePurchaseIntent(request.body)) }); } catch (error) { const message = error instanceof Error ? error.message : "INVALID_REQUEST"; response.status(message === "INVALID_REQUEST" || message.startsWith("INVALID_") ? 400 : 422).json({ error: message }); } }); app.post("/v1/transactions/:id/razorpay-order", async (request, response) => { try { const transaction = getTransactionOrThrow(request.params.id); if (transaction.state !== "policy_authorized") { if (transaction.razorpayOrderId) return response.json({ transaction, razorpayOrderId: transaction.razorpayOrderId, checkout: getPublicConfig() }); return response.status(409).json({ error: "TRANSACTION_NOT_AUTHORIZED", state: transaction.state }); } const order = await createOrder(transaction); const updated = attachRazorpayOrder(transaction.id, order.id); return response.status(201).json({ transaction: updated, order, checkout: getPublicConfig() }); } catch (error) { const message = error instanceof Error ? error.message : "RAZORPAY_ORDER_FAILED"; return response.status(message.includes("NOT_CONFIGURED") ? 503 : 422).json({ error: message }); } }); app.post("/v1/transactions/:id/checkout-started", (request, response) => { try { const transaction = getTransactionOrThrow(request.params.id); const updated = transaction.state === "checkout_started" ? transaction : markCheckoutStarted(transaction.id); return response.json({ transaction: updated, checkout: getPublicConfig() }); } catch (error) { return response.status(422).json({ error: error instanceof Error ? error.message : "CHECKOUT_START_FAILED" }); } }); app.post("/v1/transactions/:id/retry-payment", async (request, response) => { try { const original = getTransactionOrThrow(request.params.id); if (original.state !== "payment_failed") return response.status(409).json({ error: "TRANSACTION_NOT_RETRYABLE", state: original.state }); const retry = await retryFailedTransaction(original.id); return response.status(201).json({ transaction: retry, retryOfTransactionId: original.id }); } catch (error) { return response.status(422).json({ error: error instanceof Error ? error.message : "PAYMENT_RETRY_FAILED" }); } }); app.post("/v1/transactions/:id/verify-payment", async (request, response) => { try { const transaction = getTransactionOrThrow(request.params.id); const razorpayPaymentId = typeof request.body?.razorpay_payment_id === "string" ? request.body.razorpay_payment_id : ""; const razorpayOrderId = typeof request.body?.razorpay_order_id === "string" ? request.body.razorpay_order_id : ""; const razorpaySignature = typeof request.body?.razorpay_signature === "string" ? request.body.razorpay_signature : ""; if (!transaction.razorpayOrderId || transaction.razorpayOrderId !== razorpayOrderId) return response.status(400).json({ error: "RAZORPAY_ORDER_MISMATCH" }); if (!verifyCheckoutSignature(razorpayOrderId, razorpayPaymentId, razorpaySignature)) return response.status(400).json({ error: "INVALID_CHECKOUT_SIGNATURE" }); const result = await captureAndReconcile(transaction.id, razorpayPaymentId, transaction.quote.totalPaise); return response.json({ transaction: result, verified: result.state === "order_confirmed" || result.state === "payment_verified" }); } catch (error) { const message = error instanceof Error ? error.message : "PAYMENT_VERIFICATION_FAILED"; return response.status(message.includes("NOT_CONFIGURED") ? 503 : 422).json({ error: message }); } }); app.post("/v1/transactions/:id/capture", async (request, response) => { try { const transaction = getTransactionOrThrow(request.params.id); if (!transaction.razorpayPaymentId) return response.status(409).json({ error: "PAYMENT_NOT_AVAILABLE" }); const payment = await fetchPayment(transaction.razorpayPaymentId); const result = await captureAndReconcile(transaction.id, payment.id, transaction.quote.totalPaise); return response.json({ transaction: result, payment }); } catch (error) { return response.status(422).json({ error: error instanceof Error ? error.message : "CAPTURE_FAILED" }); } }); app.get("/v1/transactions/:id", (request, response) => { const transaction = getTransaction(request.params.id); if (!transaction) return response.status(404).json({ error: "TRANSACTION_NOT_FOUND" }); return response.json({ transaction }); }); app.get("/v1/transactions/:id/timeline", (request, response) => { if (!getTransaction(request.params.id)) return response.status(404).json({ error: "TRANSACTION_NOT_FOUND" }); return response.json({ events: getTimeline(request.params.id) }); }); return app; }
