"use client";
import Script from "next/script"; import { useMemo, useState } from "react";
const MERCHANT_URL = process.env.NEXT_PUBLIC_MERCHANT_URL ?? "http://localhost:3000"; const GATEWAY_URL = process.env.NEXT_PUBLIC_GATEWAY_URL ?? "http://localhost:4000"; const money = (paise: number) => new Intl.NumberFormat("en-IN", { style: "currency", currency: "INR", maximumFractionDigits: 0 }).format(paise / 100);
type Product = { id: string; name: string; category: string; pricePaise: number; rating: number; inventory: number; shortDescription: string; features: string[]; specifications: Record<string, string> };
type Quote = { quoteId: string; merchantId?: string; totalPaise: number; subtotalPaise?: number; shippingPaise?: number; taxPaise?: number; discountPaise?: number; expiresAt: string; lineItems: Array<{ productId: string; quantity: number; unitPricePaise?: number; lineTotalPaise?: number }> };
type Recommendation = { productId: string; quantity: number; rationale: string };
type Plan = { category: string; requirements: string[]; rankedProducts: Array<{ productId: string; score: number; rationale: string }>; selectedProductId: string; decisionSummary: string; basketRecommendations: Recommendation[] };
type Transaction = { id: string; state: string; razorpayOrderId?: string; razorpayPaymentId?: string; intent: { productId: string; quantity: number; maxSpendPaise: number; reason: string; lineItems?: Array<{ productId: string; quantity: number }> }; quote: Quote; policy?: { decision: "ALLOW" | "BLOCK"; checks: Array<{ rule: string; result: "PASS" | "FAIL"; observed: string; expected: string; reason: string }> } };
type RazorpayCheckout = { keyId: string; currency: "INR"; order: { id: string; amount: number; currency: string } };
export default function BuyerWorkspace() {
  const [request, setRequest] = useState("Buy the best ANC headphones under ₹5,000"); const [maxSpend, setMaxSpend] = useState(5000); const [product, setProduct] = useState<Product | null>(null); const [plan, setPlan] = useState<Plan | null>(null); const [approvedBasket, setApprovedBasket] = useState<Array<{ productId: string; quantity: number }>>([]); const [transaction, setTransaction] = useState<Transaction | null>(null); const [stage, setStage] = useState("ready"); const [error, setError] = useState(""); const [activity, setActivity] = useState<string[]>([]); const [checkout, setCheckout] = useState<RazorpayCheckout | null>(null);
  const basket = useMemo(() => approvedBasket.length ? approvedBasket : product ? [{ productId: product.id, quantity: 1 }] : [], [approvedBasket, product]); const selectedAmount = transaction?.quote.totalPaise ?? product?.pricePaise ?? 0; const remaining = Math.max(maxSpend * 100 - selectedAmount, 0); const blockedBySpend = transaction?.state === "policy_blocked" && transaction.policy?.checks.some((check) => check.rule === "MAX_SPEND" && check.result === "FAIL"); const paymentFailed = transaction?.state === "payment_failed"; const statusLabel = useMemo(() => { if (stage === "blocked") return "Blocked before payment"; if (stage === "authorized") return "Authorized"; if (stage === "working") return "Evaluating"; if (stage === "payment") return "Payment ready"; if (stage === "paid") return "Verified"; return paymentFailed ? "Payment failed" : "Ready"; }, [stage, paymentFailed]);
  async function runAgent() { setError(""); setTransaction(null); setActivity([]); setProduct(null); setPlan(null); setApprovedBasket([]); setCheckout(null); setStage("working"); try { setActivity(["Reading your constraints", "Discovering Mandate Market"]); const planResponse = await fetch("/api/agent/purchase-plan", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ request, maxSpendPaise: maxSpend * 100 }) }); const planBody = await planResponse.json() as { plan?: Plan; selectedProduct?: Product; error?: string; detail?: string }; if (!planResponse.ok || !planBody.plan || !planBody.selectedProduct) throw new Error(planBody.detail ?? planBody.error ?? "The buyer could not produce a purchase plan."); const currentPlan = planBody.plan; const chosen = planBody.selectedProduct; setPlan(currentPlan); setProduct(chosen); setActivity((current) => [...current, "Catalog loaded through merchant API", `${currentPlan.rankedProducts.length} products ranked`, `${chosen.name} selected by buyer model`]); if (currentPlan.basketRecommendations.length) setActivity((current) => [...current, `${currentPlan.basketRecommendations.length} merchant basket opportunities found`]); } catch (caught) { setStage("ready"); setError(caught instanceof Error ? caught.message : "The agent could not complete the request."); } }
  async function approveBasket(includeRecommendations: boolean) { if (!product || !plan) return; setError(""); const lines = [{ productId: product.id, quantity: 1 }, ...(includeRecommendations ? plan.basketRecommendations.map((item) => ({ productId: item.productId, quantity: item.quantity })) : [])]; const firstLine = lines[0]; if (!firstLine) { setError("The selected basket is empty."); return; } setApprovedBasket(lines); setStage("working"); setActivity((current) => [...current, includeRecommendations ? "User approved the recommended basket" : "User approved the selected product only", "Requesting a fresh combined merchant quote"]); try { const quoteResponse = await fetch(`${MERCHANT_URL}/api/agent/checkout/preview`, { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ lineItems: lines }) }); const quoteBody = await quoteResponse.json() as { quote?: Quote; error?: string }; if (!quoteResponse.ok || !quoteBody.quote) throw new Error(quoteBody.error ?? "Merchant could not produce a basket quote."); const quote = quoteBody.quote; setActivity((current) => [...current, `Basket quote obtained: ${money(quote.totalPaise)}`]); const transactionResponse = await fetch(`${GATEWAY_URL}/v1/purchase-intents`, { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ merchantId: "mandate-market", productId: firstLine.productId, quantity: firstLine.quantity, lineItems: lines, maxSpendPaise: maxSpend * 100, reason: request, quoteId: quote.quoteId }) }); const body = await transactionResponse.json() as { transaction?: Transaction; error?: string }; if (!transactionResponse.ok || !body.transaction) throw new Error(body.error ?? "Gateway rejected the purchase intent."); const createdTransaction = body.transaction; setTransaction(createdTransaction); if (createdTransaction.state === "policy_authorized") { setActivity((current) => [...current, "Gateway revalidated every basket line", "Policy approved the combined transaction"]); setStage("authorized"); } else { setActivity((current) => [...current, "Policy blocked the basket", "No Razorpay order was created"]); setStage("blocked"); } } catch (caught) { setStage("ready"); setError(caught instanceof Error ? caught.message : "Basket approval failed."); } }
  async function prepareRazorpayCheckout() { if (!transaction || transaction.state !== "policy_authorized") return; setError(""); try { setActivity((current) => [...current, "Creating Razorpay Test Mode order"]); const response = await fetch(`${GATEWAY_URL}/v1/transactions/${transaction.id}/razorpay-order`, { method: "POST" }); const body = await response.json() as { transaction?: Transaction; order?: { id: string; amount: number; currency: string }; checkout?: { keyId: string; currency: "INR" }; error?: string }; if (!response.ok || !body.transaction || !body.order || !body.checkout) throw new Error(body.error ?? "Unable to create the Razorpay Test Mode order."); const nextTransaction = body.transaction; const nextOrder = body.order; const nextCheckout = body.checkout; setTransaction(nextTransaction); setCheckout({ keyId: nextCheckout.keyId, currency: nextCheckout.currency, order: nextOrder }); await fetch(`${GATEWAY_URL}/v1/transactions/${transaction.id}/checkout-started`, { method: "POST" }); setStage("payment"); setActivity((current) => [...current, `Razorpay order ${nextOrder.id} created`]); } catch (caught) { setError(caught instanceof Error ? caught.message : "Razorpay checkout could not be prepared."); } }
  async function retryPayment() { if (!transaction || transaction.state !== "payment_failed") return; setError(""); setCheckout(null); setStage("working"); setActivity((current) => [...current, "Starting a new payment attempt", "Revalidating basket, price, inventory and spending policy"]); try { const response = await fetch(`${GATEWAY_URL}/v1/transactions/${transaction.id}/retry-payment`, { method: "POST" }); const body = await response.json() as { transaction?: Transaction; error?: string }; if (!response.ok || !body.transaction) throw new Error(body.error ?? "Unable to start a payment retry."); const retryTransaction = body.transaction; setTransaction(retryTransaction); setActivity((current) => [...current, "New transaction created", "Original failed attempt preserved in the audit trail"]); if (retryTransaction.state === "policy_authorized") { setStage("authorized"); setActivity((current) => [...current, "Policy approved the new attempt"]); } else setStage("blocked"); } catch (caught) { setStage("ready"); setError(caught instanceof Error ? caught.message : "Payment retry failed."); } }
  function launchRazorpay() { if (!transaction || !checkout) return; const RazorpayCtor = (window as unknown as { Razorpay?: new (options: Record<string, unknown>) => { open: () => void } }).Razorpay; if (!RazorpayCtor) { setError("Razorpay Checkout script is still loading. Try again in a moment."); return; } const instance = new RazorpayCtor({ key: checkout.keyId, amount: checkout.order.amount, currency: checkout.currency, name: "Mandate Market", description: transaction.intent.reason, order_id: checkout.order.id, theme: { color: "#171717" }, handler: async (response: { razorpay_payment_id: string; razorpay_order_id: string; razorpay_signature: string }) => { setActivity((current) => [...current, "Checkout returned payment identifiers", "Verifying payment on the gateway"]); const verifyResponse = await fetch(`${GATEWAY_URL}/v1/transactions/${transaction.id}/verify-payment`, { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(response) }); const body = await verifyResponse.json() as { transaction?: Transaction; verified?: boolean; error?: string }; if (!verifyResponse.ok || !body.transaction) { setError(body.error ?? "Payment verification failed."); return; } const verifiedTransaction = body.transaction; setTransaction(verifiedTransaction); setStage(body.verified ? "paid" : "payment"); setActivity((current) => [...current, body.verified ? "Payment verified by gateway" : "Payment is awaiting capture"]); }, modal: { ondismiss: () => setActivity((current) => [...current, "Checkout closed before completion"]) } }); instance.open(); }
  return <main className="buyer-shell"><Script src="https://checkout.razorpay.com/v1/checkout.js" strategy="afterInteractive" /><header className="buyer-header"><div className="brand"><span className="brand-mark">M</span><span>MANDATE</span></div><div className="header-right"><span className="live-dot" /> Agent runtime <span className="divider" /> Razorpay Test Mode</div></header><section className="buyer-grid"><section className="request-column"><div className="section-kicker">AI BUYER</div><h1>Purchase workspace</h1><p className="lede">The buyer can find revenue opportunities, but every basket change stays under explicit user approval and the gateway policy.</p><label className="field-label" htmlFor="request">Request</label><textarea id="request" value={request} onChange={(event) => setRequest(event.target.value)} /><div className="limit-row"><div><label className="field-label" htmlFor="limit">Maximum spend</label><div className="currency-input"><span>₹</span><input id="limit" type="number" min={0} step={100} value={maxSpend} onChange={(event) => setMaxSpend(Number(event.target.value) || 0)} /></div></div><div className="policy-note"><strong>Hard limit</strong><span>Owned by the user policy, not the buyer model.</span></div></div><button className="run-button" onClick={() => void runAgent()} disabled={stage === "working" || !request.trim()}>{stage === "working" ? "Evaluating…" : "Evaluate purchase"}<span>→</span></button>{plan?.basketRecommendations.length && !transaction ? <div className="basket-approval"><div className="panel-kicker">MERCHANT OPPORTUNITIES</div><strong>Increase the basket?</strong><p>The merchant exposed complementary products through its agent-ready revenue API. Nothing is added until you approve it.</p>{plan.basketRecommendations.map((item) => <div className="basket-option" key={item.productId}><div><code>{item.productId}</code><span>{item.quantity} × recommended</span></div><small>{item.rationale}</small></div>)}<div className="basket-actions"><button onClick={() => void approveBasket(false)}>Selected product only</button><button className="primary" onClick={() => void approveBasket(true)}>Approve recommended basket →</button></div></div> : null}{transaction?.state === "policy_authorized" && !checkout ? <button className="pay-button" onClick={() => void prepareRazorpayCheckout()}>Create Razorpay Test Order <span>→</span></button> : null}{checkout && stage === "payment" ? <button className="pay-button pay-ready" onClick={launchRazorpay}>Open Razorpay Test Checkout <span>↗</span></button> : null}{paymentFailed ? <button className="pay-button" onClick={() => void retryPayment()}>Try payment again <span>↻</span></button> : null}{error ? <div className="error-box">{error}</div> : null}<div className="workspace-footer"><span>Merchant: Mandate Market</span><span>Payment authority: gateway only</span></div></section><section className="decision-column"><div className="decision-header"><div><span className={`status status-${stage}`}>{statusLabel}</span><span className="decision-label">Current transaction</span></div>{transaction ? <code>{transaction.id}</code> : <span className="quiet">No transaction yet</span>}</div>{blockedBySpend ? <div className="block-banner"><div><span className="block-eyebrow">POLICY GATE</span><strong>Purchase stopped before Razorpay</strong><span>The verified basket total exceeds the user's hard spending limit.</span></div><span className="block-badge">NO ORDER CREATED</span></div> : null}{paymentFailed ? <div className="block-banner failure-banner"><div><span className="block-eyebrow">PAYMENT RECOVERY</span><strong>Payment attempt failed</strong><span>No merchant order was created. Retry creates a new transaction and re-runs the policy gate.</span></div><span className="block-badge">SAFE TO RETRY</span></div> : null}<div className="product-panel"><span className="panel-kicker">SELECTED PRODUCT</span>{product ? <><div className="product-main"><div><h2>{product.name}</h2><p>{product.shortDescription}</p></div><strong>{money(selectedAmount)}</strong></div><div className="feature-list">{product.features.slice(0, 4).map((feature) => <span key={feature}>✓ {feature}</span>)}</div>{plan ? <div className="model-reason"><span>BUYER MODEL</span><p>{plan.decisionSummary}</p></div> : null}</> : <div className="empty-selection"><span>—</span><p>Run the buyer to see a live product decision.</p></div>}</div>{basket.length > 1 ? <div className="basket-panel"><div className="panel-heading"><span className="panel-kicker">APPROVED BASKET</span><span>{basket.length} lines</span></div>{basket.map((line) => <div className="basket-line" key={line.productId}><code>{line.productId}</code><span>× {line.quantity}</span></div>)}</div> : null}<div className="policy-panel"><div className="panel-heading"><span className="panel-kicker">POLICY</span><span>{remaining ? `${money(remaining)} remaining` : selectedAmount ? "Limit reached" : "Awaiting evaluation"}</span></div><div className="policy-grid"><div><span>Maximum spend</span><strong>{money(maxSpend * 100)}</strong></div><div><span>Transaction</span><strong>{selectedAmount ? money(selectedAmount) : "—"}</strong></div></div><div className="check-list">{transaction?.policy?.checks.map((check) => <div className="check-row" key={check.rule}><span className={`check ${check.result === "PASS" ? "pass" : "fail"}`}>{check.result === "PASS" ? "✓" : "×"}</span><div><strong>{check.rule.replaceAll("_", " ")}</strong><small>{check.reason}</small></div><code>{check.observed}</code></div>) ?? <div className="check-placeholder">Policy checks appear here after gateway revalidation.</div>}</div></div>{transaction?.state === "policy_blocked" ? <div className="error-box"><strong>Payment not initiated.</strong><br />The policy engine stopped this purchase before a Razorpay Order was created.</div> : null}<div className="activity-panel"><div className="panel-heading"><span className="panel-kicker">ACTIVITY</span><span>Application events</span></div><div className="timeline">{(activity.length ? activity : ["Waiting for a request"]).map((item, index) => <div className="timeline-row" key={`${item}-${index}`}><span className={`timeline-dot ${activity.length ? "done" : ""}`} /><span>{item}</span></div>)}</div></div>{transaction?.razorpayOrderId || transaction?.razorpayPaymentId ? <div className="refs-panel"><div><span>Razorpay order</span><code>{transaction.razorpayOrderId ?? "—"}</code></div><div><span>Payment</span><code>{transaction.razorpayPaymentId ?? "—"}</code></div></div> : null}</section></section></main>;
}
